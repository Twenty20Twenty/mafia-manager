# ─── Database ──────────────────────────────────────────────────────────────────
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=

# ─── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=
JWT_EXPIRATION_MS=

# ─── Mail — используем Brevo (порт 587 не заблокирован) ───────────────────────
# Зарегистрируйся на brevo.com, получи SMTP ключ
# Пока оставляем mailpit для теста — потом заменим
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS=false

APP_MAIL_FROM=
APP_FRONTEND_URL=

# ─── MinIO ────────────────────────────────────────────────────────────────────
MINIO_ENDPOINT=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_BUCKET_AVATARS=
# Публичный URL — браузер скачивает аватары напрямую с порта 9000
MINIO_PUBLIC_URL=